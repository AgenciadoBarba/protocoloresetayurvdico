const TopBar = () => (
  <div className="bg-sage-dark px-5 py-2.5 text-center text-xs font-semibold text-primary-foreground/75 leading-relaxed">
    🌿 <span className="text-gold-light font-bold">OFERTA ESPECIAL:</span> Acesso completo com todos os bônus — por tempo limitado
  </div>
);

export default TopBar;
